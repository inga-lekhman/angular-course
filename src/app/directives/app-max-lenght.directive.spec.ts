import { MaxLenghtDirective } from './app-max-lenght.directive';

describe('AppMaxLenghtDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxLenghtDirective();
    expect(directive).toBeTruthy();
  });
});
